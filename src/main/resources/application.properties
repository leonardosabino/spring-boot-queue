server.port=8081

kafka.enable=false
kafka.url=http://localhost:9092
kafka.topic=example-topic
kafka.groupId=exampleGroupId

sqs.enable=false
sqs.name=example-sqs
sqs.url=http://localhost:4576
sqs.urlName=${sqs.url}/queue/${sqs.name}